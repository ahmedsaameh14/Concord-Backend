const PROJECT_SERVICES = Object.freeze([
  'construction',
  'infrastructure',
  'transportation',
]);

const DEFAULT_PAGE = 1;
const DEFAULT_LIMIT = 12;
const MAX_LIMIT = 50;

module.exports = {
  PROJECT_SERVICES,
  DEFAULT_PAGE,
  DEFAULT_LIMIT,
  MAX_LIMIT,
};
